apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: archive-me-
  labels:
    workflows.argoproj.io/archive-strategy: "always"
spec:
  entrypoint: whalesay
  templates:
  - name: whalesay
    container:
      image: docker/whalesay:latest
      command: [cowsay]
      args: ["hello world"]
